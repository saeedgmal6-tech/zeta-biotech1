# ZETA BIOTECH Website

Static ZETA BIOTECH company website for deployment with Cloudflare Workers Static Assets.

Deploy command:
`npx wrangler deploy`

The website files are intentionally kept at the repository root so the Cloudflare build works even when the GitHub repository is uploaded file-by-file.
